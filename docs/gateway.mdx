# Gateway

The PayNow API is only reachable by an HTTP gateway for REST requests using JSON.

## Base URL

The base URL for the API is **`https://paynow.gg/api/v1`**

All routes in our API documentation are relative to this route.

---

## Ping the API {{ tag: 'GET' }}

<Row>
  <Col>
    Sends back a basic text response from the API.

    ### Returns
    Returns `text/html` that says `PayNow API`

  </Col>
  <Col sticky>
    <Endpoints request>
        <Endpoint method="GET" url="/" />
    </Endpoints>

    ```yaml {{ title: 'Response' }}
    PayNow API
    ```
  </Col>
</Row>

